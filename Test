import React from "react";

const riders = [
  {
    name: "Eddy Merckx",
    country: "Belgium",
    score: 94,
    stats: { grandTours: 11, classics: 19, wins: 525 },
    image: "https://upload.wikimedia.org/wikipedia/commons/5/5f/Eddy_Merckx_1973.jpg",
  },
  {
    name: "Tadej Pogacar",
    country: "Slovenia",
    score: 89,
    stats: { grandTours: 4, classics: 6, wins: 60 },
    image: "https://upload.wikimedia.org/wikipedia/commons/8/8c/Tadej_Poga%C4%8Dar_2021.jpg",
  },
];

function RiderCard({ rider }) {
  return (
    <div className="bg-white rounded-2xl shadow-sm p-4 w-full max-w-sm">
      <img
        src={rider.image}
        alt={rider.name}
        className="w-full h-64 object-cover rounded-xl grayscale contrast-110 brightness-105"
      />

      <div className="mt-4">
        <h2 className="text-xl font-semibold">{rider.name}</h2>
        <p className="text-sm text-gray-500">{rider.country}</p>

        <div className="mt-3 bg-yellow-100 inline-block px-3 py-1 rounded-lg text-sm font-medium">
          GOAT Score: {rider.score}
        </div>

        <div className="mt-4 space-y-2 text-sm">
          <Stat label="Grand Tours" value={rider.stats.grandTours} />
          <Stat label="Classics" value={rider.stats.classics} />
          <Stat label="Wins" value={rider.stats.wins} />
        </div>
      </div>
    </div>
  );
}

function Stat({ label, value }) {
  return (
    <div>
      <div className="flex justify-between">
        <span>{label}</span>
        <span className="font-medium">{value}</span>
      </div>
      <div className="w-full h-1 bg-gray-200 rounded-full mt-1">
        <div
          className="h-1 bg-black rounded-full"
          style={{ width: `${Math.min(value * 2, 100)}%` }}
        />
      </div>
    </div>
  );
}

export default function App() {
  return (
    <div className="min-h-screen bg-[#FAFAFA] text-[#111]">
      {/* HERO */}
      <section className="text-center py-20 px-4">
        <h1 className="text-4xl md:text-5xl font-serif mb-4">
          Who is the greatest cyclist of all time?
        </h1>
        <p className="text-gray-600 max-w-xl mx-auto mb-6">
          Compare legends across eras using data, context and achievements.
        </p>

        <div className="flex justify-center gap-4">
          <button className="bg-black text-white px-6 py-3 rounded-xl">
            Compare riders
          </button>
          <button className="border px-6 py-3 rounded-xl">
            Explore rankings
          </button>
        </div>
      </section>

      {/* COMPARISON */}
      <section className="flex flex-col md:flex-row justify-center items-center gap-10 px-6">
        {riders.map((r) => (
          <RiderCard key={r.name} rider={r} />
        ))}
      </section>

      {/* INSIGHT */}
      <section className="max-w-2xl mx-auto text-center py-16 px-6">
        <p className="text-lg text-gray-700">
          Merckx remains unmatched in total dominance and versatility, while
          Pogacar shows exceptional efficiency in the modern era.
        </p>

        <div className="mt-6 inline-flex border rounded-xl overflow-hidden">
          <button className="px-4 py-2 bg-gray-100">Raw stats</button>
          <button className="px-4 py-2 bg-yellow-100">Era-adjusted</button>
        </div>
      </section>

      {/* EXPLORE */}
      <section className="max-w-5xl mx-auto px-6 pb-20">
        <h3 className="text-xl font-semibold mb-6 text-center">
          Compare iconic rivalries
        </h3>

        <div className="grid md:grid-cols-3 gap-4">
          {["Merckx vs Hinault", "Pogacar vs Vingegaard", "Big 5 All-Time"].map(
            (item) => (
              <div
                key={item}
                className="bg-white p-4 rounded-xl shadow-sm text-center"
              >
                {item}
              </div>
            )
          )}
        </div>
      </section>
    </div>
  );
}
